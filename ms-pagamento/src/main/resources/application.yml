server:
  port: 8080

spring:
  application:
    name: ms-pagamento
  datasource:
    url: jdbc:postgresql://localhost:5432/db_pagamento
    driver-class-name: org.postgresql.Driver
    username: pagamento
    password: pagamento123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

app:
  comprovantes:
    url: http://localhost:8081/comprovantes

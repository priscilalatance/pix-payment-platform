spring:
  datasource:
    url: jdbc:h2:mem:db_pagamento_test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

app:
  comprovantes:
    url: http://localhost:8081/comprovantes
